-- 扩容 schema_migrations.version 字段长度，支持更长的迁移文件名
ALTER TABLE schema_migrations ALTER COLUMN version TYPE VARCHAR(64);

-- 黄金数据集表新增评测字段
ALTER TABLE golden_dataset
    ADD COLUMN IF NOT EXISTS retrieved_chunk_ids TEXT[] NOT NULL DEFAULT '{}',
    ADD COLUMN IF NOT EXISTS is_hit BOOLEAN DEFAULT NULL,
    ADD COLUMN IF NOT EXISTS hit_rank INT DEFAULT NULL,
    ADD COLUMN IF NOT EXISTS evaluated_at TIMESTAMPTZ DEFAULT NULL;

-- 项目表新增评测汇总字段
ALTER TABLE project
    ADD COLUMN IF NOT EXISTS eval_recall_at_10 FLOAT DEFAULT NULL,
    ADD COLUMN IF NOT EXISTS eval_mrr FLOAT DEFAULT NULL,
    ADD COLUMN IF NOT EXISTS eval_answerable INT DEFAULT NULL,
    ADD COLUMN IF NOT EXISTS eval_total INT DEFAULT NULL,
    ADD COLUMN IF NOT EXISTS eval_latency_avg_ms FLOAT DEFAULT NULL,
    ADD COLUMN IF NOT EXISTS evaluated_at TIMESTAMPTZ DEFAULT NULL;
